# Design QA：奶油云朵信箱全站「云层传送门」

## 对照基准

- source visual truth path: `C:\Users\Administrator\.codex\generated_images\01a042c7-0a14-7e03-8f57-958665a71a50\exec-9caf0726-f1c7-409b-b2e0-7eb85befcbcc.png`
- source pixels: 1487 × 1058
- implementation screenshot path: Codex In-app Browser 当前任务内联截图；浏览器接口未暴露可复用的本地截图路径
- implementation URL: `http://127.0.0.1:5173/`（保留的主预览）
- desktop viewport: 1440 × 1024，device density 1
- mobile viewport: 390 × 844，device density 1
- normalization: 参考稿与实现均按相同桌面首屏比例判断视觉语言；长页另外检查顶部、内容中段和页脚，任务型页面检查完整可见布局
- state: 主页、小贴士、关于页为管理员登录状态；管理员工作台与完整会话使用本地演示数据；登录、注册页面使用同源第二端口的无会话状态

## Findings

- 无剩余 P0、P1、P2。全站已统一使用同一云层传送门世界、暖奶油色板、桃橙强调色与分层亚克力材质。
- P3：客户端工作台没有在本轮切换账号单独截图，避免改动用户当前管理员会话；其组件与管理员/聊天页共用本轮已验收的 `app-shell`、`app-header`、表单与面板视觉规则。

## 完整视图对照

参考稿与实现均已打开并视觉检查。首页继续保持参考稿的左文案、右侧角色场景和底部信任栏。内页没有复制首页的大标题构图，而是把参考稿的“云层世界 + 悬浮亚克力界面”抽象为统一系统：背景固定在视口，真实页面内容在其上滚动，标题、卡片、页头、页脚、会话列和表单根据任务密度使用不同透明度。

浏览器实际检查了 `/`、`/tips`、`/about`、`/login`、`/register`、`/admin`、`/chat/conv-demo-1`。长页滚动至底部后，云层世界仍连续覆盖，页脚没有再落到纯色空白区。

## 聚焦区域对照

- 公开内容页：标题采用独立亚克力舞台，卡片和隐私模块使用更浅的半透明材质；传送门仍是明确焦点，但不影响长文阅读。
- 认证页：左右两块大型毛玻璃面板悬浮于传送门前，输入框、按钮、演示账号入口都维持原有可用性；登录与注册均无横向溢出。
- 管理员工作台：桌面保留三栏；手机端改为“会话 / 对话 / AI”三视图，底部亚克力导航固定可达，输入区停留在对话视图底部。收件箱、消息区、详情与 AI 分析面板降低背景饱和度并增强局部模糊，避免梦幻背景压过工作内容。
- 完整会话：管理员消息继续在右侧，AI 分析栏持续可见；消息气泡、输入区和侧栏改为不同层级的亚克力表面。
- 手机端：管理员工作台在 390 × 844 下逐一检查会话、对话、AI 三个视图；页面及消息区均满足 `scrollWidth === clientWidth`，消息操作菜单打开后也不会让气泡横向漂移。

## 必检视觉面

- 字体与排版：现有中文字体栈、字号和字重层级保留；桌面与手机端标题换行自然，表单标签、消息正文和 AI 小字仍可读。
- 间距与布局：桌面公开页最大宽度、三栏工作台和会话布局未改变；移动断点继续使用原有单列结构。页头与页脚统一为圆角亚克力条。
- 颜色与视觉变量：奶油白、桃橙、可可棕、鼠尾草绿与暖金云光映射一致；任务型页面降低背景饱和度和透明度以维持对比。
- 图片质量与资产：背景使用 1920 × 1280 WebP，小熊信箱使用 1536 × 1024 RGBA WebP；无拉伸、破损透明边或资源加载错误。
- 文案与内容：路由、按钮、隐私说明、心理健康边界、紧急求助信息、AI 提示和会话数据均未改写。

## 交互与运行检查

- 页面导航、小贴士、关于页、管理员工作台和完整会话可正常打开。
- 管理员点击“打开完整会话”后，右侧 AI 分析仍显示。
- 引用回复已实际发送并显示引用摘要；刚发送的管理员消息可在两分钟内撤回，并显示“你撤回了一条消息”。
- 撤回消息不会继续展示其引用摘要；会话列表以“[消息已撤回]”作为安全预览。
- 发送失败保留原文、图片和引用上下文供一键重试；图片发送展示校验、处理、上传三个阶段。
- 若被撤回消息是 AI 分析来源，客户端演示会清除对应分析，服务端查询也会排除该来源。
- `/tips`、`/about`、`/login`、`/register`、`/admin` 和 `/chat/conv-demo-1` 桌面端无横向溢出。
- `/about` 与 `/admin` 的 390 × 844 响应式检查无横向溢出。
- 全站增加“跳到主要内容”键盘入口；实测触发后焦点落在 `main#main-content`，路由切换会通过礼貌播报提示当前页面，并把焦点移入主要内容。
- 公开页、用户端与管理员端均提供持久化阅读辅助菜单：大号文字、高对比和减少动效可独立启用或一键恢复；519px 移动视口三项同时开启时页面仍为 `scrollWidth === clientWidth`，聊天正文计算字号为 17px。
- 聊天记录改为具名 `role="log"` 区域，新增消息采用礼貌播报；日期为三级标题，每条消息包含“我 / 倾听员 + 时间”的独立可访问名称。管理员收件箱未读数与 AI 授权状态也提供状态播报。
- 对比度与阅读密度已人工复核：主按钮和本人消息气泡从白字桃色改为深棕字浅桃色，正文行高增加，关键辅助文字与消息时间提升至 11.5–12px，手机底部导航标签提升至 12px，微操作触控面扩大至约 40px。
- 固定背景与云雾层使用独立 22s/13s 动画，页面滚动时不会在内容中段切成纯色。
- `prefers-reduced-motion: reduce` 统一关闭动画与过渡。
- 浏览器验收曾捕获完整会话的 `recallingId` 未定义回归；状态移入 `ChatPage` 后重新载入，完整会话、消息操作和 AI 侧栏均恢复显示，修复后没有新增控制台错误。
- 41 项自动化测试通过；Vite 生产构建通过；新增 Netlify Functions 通过 Node 语法检查；`git diff --check` 无内容错误，仅有 Windows 换行提示。
- AI 核心摘要的“放入回复草稿”已在手机端实测：界面自动回到对话页，草稿框出现推荐文案，消息数仍为 4，未触发发送；页面 `horizontalOverflow === false`。
- 收件箱已实测搜索空状态、置顶切换、打开会话后未读数从 2 归零、标签选择与列表回显；手机端收件箱保持无横向溢出。
- 本地高风险测试消息已触发“立即复核”；人工复核、安全询问、校内支持三项完成前“完成交接”不可用，完成后状态依次从“人工复核中”到“已连接支持”“已完成交接”，操作记录显示 4 条时间戳事件。
- 倾诉者完整会话已实测“本次会话隐私”卡：明确显示 AI 授权状态、实际分析范围、仅管理员可见边界和 90 天保留期；移动视口为 452 × 809，页面 `scrollWidth === clientWidth`。永久删除弹窗在输入不完整时保持禁用，只有完整输入“删除”才解锁；验收后选择“暂不删除”，现有演示会话未被删除。

## 比较历史

1. P2 已解决：首页之外只有弱云雾，无法识别为同一风格。所有公开页、认证页、客户端/管理员工作区和聊天页现共享传送门背景与亚克力材质。
2. P2 已解决：首次全站背景规则的负边距引起桌面端 `scrollWidth` 从 1425px 增至 1676px。根节点增加横向裁切后复查为 1425px。
3. P2 已解决：旧的 `height: min(620px, 62vh)` 仍限制内页背景，造成下半页纯色断层；首页也保留 930px 高度。背景层改为贯穿视口的固定层，长页底部重新截图后云层连续。
4. P2 已解决：路由入场动画的 `forwards/both` 填充让 `transform: matrix(...)` 残留，使固定背景被当作长文档元素并额外撑高页面。改为 `backwards` 后计算样式恢复 `transform: none`，文档高度与页脚位置正常。
5. P2 已解决：任务型页面直接透出高对比传送门会干扰阅读。管理员与聊天界面降低背景饱和度，消息区、侧栏和 AI 面板增强局部毛玻璃遮罩。
6. P1 已解决：完整会话初次验收出现空白页，原因是撤回状态误放在用户工作台组件中；状态已归位到 `ChatPage` 并完成重新验收。
7. P2 已解决：手机端消息菜单从气泡外侧展开时撑宽滚动容器，点击后消息整体横向漂移；菜单改为向内容内侧展开，消息区复查为 375 / 375px。
8. P2 已解决：手机底部三入口使用深色独立描边，视觉上像系统按钮，与全站奶油亚克力材质割裂；已改为单一悬浮玻璃底座、无描边纵向标签和桃色柔光选中态。
9. P2 已解决：“引用回复”与引用预览关闭按钮继承浏览器默认边框，和新版底栏再次产生割裂；两者已显式去除系统外观，分别改为暖色轻操作与半透明圆形关闭控件。
10. P2 已解决：AI 分析信息分散，管理员需在多段文字中寻找情绪、风险与回应建议；现改为固定顺序的三张核心摘要卡，移动端 AI 入口优先展示该面板，匿名资料下移，并明确“一键填入但不自动发送”。
11. P2 已解决：会话列表只能按更新时间浏览，无法快速识别未读、重点或高风险会话；现加入搜索、未读筛选、管理员私有置顶与标签，并按“置顶 > 风险 > 未读 > 最近更新”排序，列表同步展示最后发送方和时间。
12. P1 已解决：高风险 AI 提示只有警告文案，没有可确认的人工处置路径；现加入五步危机处理清单、完成交接门槛、现实支持号码和追加式操作记录，AI 无权自动勾选或宣告风险解除。
13. P1 已解决：倾诉者无法集中了解 AI 到底读取什么、数据保存多久，也没有删除整段会话的自主入口；现加入会话级隐私状态卡、随时撤回 AI 授权、90 天关闭会话清理与本人专属永久删除。私密图片采用删除队列重试，管理员无权代删。
14. P2 已解决：帮助文字、消息元信息和手机底部导航字号偏小，部分桃色按钮与本人消息使用白字导致阅读吃力，且 SPA 没有跳过导航和路由焦点管理；现已统一提升阅读密度与对比度，并加入可持久化阅读辅助、跳转主内容、路由播报、明确标题层级和聊天日志语义。

## Implementation Checklist

- [x] 统一公开页页头、标题区、内容卡片和页脚
- [x] 统一登录、注册、找回密码与邀请页面的认证面板
- [x] 统一客户端工作台、管理员工作台和完整会话
- [x] 保持管理员完整会话中的 AI 分析栏
- [x] 修复全站横向溢出与背景高度断层
- [x] 检查桌面与移动端、控制台、测试和生产构建
- [x] 手机管理员会话 / 对话 / AI 三视图与固定输入区
- [x] 两分钟撤回、引用回复、失败重试与图片发送阶段反馈
- [x] AI 情绪 / 风险 / 推荐回应三项核心摘要与安全草稿填入
- [x] 会话搜索、未读、置顶、风险优先、标签与最后回复时间
- [x] 高风险人工复核、校内交接、现实支持与操作记录
- [x] 用户端 AI 授权透明度、保留期限、本人永久删除与私密图片清理队列
- [x] 全站键盘跳转、路由焦点与播报、聊天日志语义、阅读辅助和移动端可读性

## Follow-up Polish

- P3：未来可为客户端工作台制作一张专属“云端书桌”插画，但当前复用背景更轻、更一致，也不会增加加载成本。
- P3：本轮为浏览器与辅助功能树级验收，尚未进行真实屏幕阅读器设备测试、专业色盲模拟或完整 WCAG/axe 合规审计，因此不声明完整无障碍认证。

final result: passed
