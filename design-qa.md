# Design QA：奶油云朵信箱「云端微光」

## 对照基准

- source visual truth path: `C:\Users\Administrator\.codex\generated_images\01a042c7-0a14-7e03-8f57-958665a71a50\exec-ed19c173-8617-4cdf-a2a9-aa0a0665fc03.png`
- source pixels: 1487 × 1058
- implementation screenshot URL: `http://127.0.0.1:5173/`，由 Codex In-app Browser 在当前任务中捕获并逐屏检查
- implementation viewport and pixels: 1440 × 1024，device density 1
- normalization: 两者宽高比近似一致；以 1440 × 1024 的实现视口判断首屏构图，并另以 390 × 844 检查响应式结果
- state: 本地管理员会话已登录，首页内容与未登录状态一致，仅页头账号入口不同

## 完整视图对照

参考稿与浏览器实现均已在当前任务中打开。实现保留真实文字、导航、按钮和小熊信箱插画，以独立的水彩云雾素材替代参考稿中不可交互的合成背景。首屏完成两行标题、左右分栏、柔光云层、插画重心和半透明信任卡片的对应；下半页继续使用原有内容结构，没有把参考图当作整页背景贴图。

## 聚焦区域对照

- Hero：检查了标题换行、CTA 尺寸、插画比例、云层裁切和前景光点。最终桌面与移动端标题均为两行。
- 信任区：三项信息改为独立半透明卡片，保持原文与图标语义，层次接近参考稿。
- 小贴士：检查了标签、标题、正文、留白和新增线性装饰图标；六张卡片在桌面端无裁切或横向溢出。
- 未额外放大页脚区域，因为其排版未受参考稿影响，浏览器整页滚动检查未发现断层。

## 必检视觉面

- 字体与排版：继续使用现有中文系统字体栈；字重、行高和层级清晰。桌面和手机 Hero 均为两行，正文无截断。
- 间距与布局：1440 宽度下 Hero、信任区和卡片网格对齐；390 宽度下为单列且 `scrollWidth === clientWidth`。
- 颜色与视觉变量：奶油白、蜜桃橙、可可棕和少量鼠尾草绿保持一致；新增云层只使用低对比蓝、桃和奶油色。
- 图片质量与资产：使用真实 WebP 水彩背景和原有透明小熊插画，没有用 CSS 图形伪造插画；背景资源 1536 × 1024，加载成功。
- 文案与内容：现有文案、隐私声明、紧急求助信息和 CTA 未更改。

## 交互与运行检查

- 首页“小贴士”入口可跳转至 `/tips`。
- 390 宽度下移动导航可展开。
- 背景后层动画为 `cloud-atmosphere-drift`（18s），前层为反向的 `cloud-atmosphere-breathe`（11s），线稿云朵与光点使用独立节奏；小熊以 5.6s 周期轻柔浮动。鼠标移动与页面滚动会驱动整体环境层的轻微视差。
- 动态可感知性实测：桌面端 1.8 秒内前景云层位移约 45px、后景约 22px、小熊约 30px；移动端 1.6 秒内前景云层约 13px，足以在正常浏览中察觉层次而不影响阅读。
- `prefers-reduced-motion: reduce` 规则会统一关闭动画与过渡。
- 首页与小贴士页背景图片加载成功；浏览器控制台 error 日志为空。
- 26 项自动化测试通过，生产构建通过。

## 比较历史

1. P2：1440 宽度下 Hero 标题被挤成三行。修正网格比例并把桌面字号上限调整为 70px；复查后为两行。
2. P2：390 宽度下桌面字号规则覆盖移动样式，标题成为三行。增加 42px 移动端覆盖；复查后为两行。
3. P2：非首页的呼吸背景缩放造成横向滚动条。为公开页面容器增加横向裁切；复查 `/tips` 后 `scrollWidth === clientWidth`。
4. 最终复查：桌面与移动端无 P0、P1、P2 问题，浏览器无错误。
5. P2：首版云层在 30 秒内仅移动约 3%，缓动起点又接近静止，用户难以察觉变化。将前后景改为 18s/11s 反向位移，增加三枚线稿云朵、可见光点轨迹、小熊浮动及指针/滚动视差；1440 × 1024 与 390 × 844 复查均可在 2 秒内感知位移，无横向溢出或控制台错误。

## Follow-up Polish

- P3：参考稿的云层更密、更梦幻；实现主动降低了背景密度，以保证真实中文内容、较小屏幕和长时间浏览时的可读性。

final result: passed
